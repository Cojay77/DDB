// Fichier factice pour éviter erreur 404
// Redirige vers le vrai SW s'il est chargé en doublon

self.addEventListener('install', () => {
  console.log("✅ Faux firebase-messaging-sw.js à la racine chargé.");
});

